@model IEnumerable<WeChatHelloWorld1.Models.CustomerOrderStatue>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerOrderID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Statue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpdateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerOrderID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Statue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UpdateDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatueID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatueID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatueID })
        </td>
    </tr>
}

</table>
